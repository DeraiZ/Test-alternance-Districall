{% extends 'base.html.twig' %}


{% block body %}
    <div class="example-wrapper">
        <div class="new_user_form">
    <form class="form form_container" enctype="multipart/form-data" method="post">
        <div class="newUser userEmail">
            {{ form_label(form.email, 'Mail' ) }}
            {{ form_widget(form.email) }}
        </div>
        <div class="newUser userPassword">
            {{ form_label(form.password, 'Mot de passe' ) }}
            {{ form_widget(form.password) }}
        </div>
        <div class="newUser userPassword">
            {{ form_label(form.age, 'Votre âge' ) }}
            {{ form_widget(form.age) }}
        </div>
        <div class="newUser userPassword">
            {{ form_label(form.sex, 'Sexe' ) }}
            {{ form_widget(form.sex) }}
        </div>
        <div class="conexion-btn">
            <button type="submit" class="btn btn-dark send_btn">Sign In</button>
        </div>
        {{ form_rest(form) }}
    </form>
        </divclass>
    </div>
{% endblock %}

